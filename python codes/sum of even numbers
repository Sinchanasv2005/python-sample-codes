num = int(input("Enter any number: "))
i = 1
total = 0                    # this stores the sum

while i <= num:
    if i % 2 == 1:
        total = total + i       # add current even i to the running sum
    i = i + 1             # don’t forget to increment i

print("Sum of even numbers is:",total)
